"0","```r
dates <- corpus_df$date
dates <- mdy(dates)
i_month <- month(dates)
i_year <- year(dates)
ym <- 
  paste(i_year, i_month, \"01\", sep = \"-\") %>% 
  ymd() %>% 
  as.numeric()

corpus_df <- 
corpus_df %>% 
  mutate(ym = ym) 

```"
